<%= form_for(pet) do |f| %>
  <% if pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% pet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :alive, style: 'display:none' %>
    <%= f.check_box :alive, style:'display:none' %>
  </div>

  <div class="field">
    <%= f.label :date_of_death %>
    <%= f.datetime_select :date_of_death %>
  </div>

  <div class="field">
    <%= f.label :user_id, style: 'display:none'%>
    <%= f.text_field :user_id, :value => session[:user_id],
    style: 'display:none' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn-info' %>
  </div>
<% end %>
